<?xml version="1.0" encoding="UTF-8"?>
<project name="pomodoro" default="build">
    
    <target name="bla">
        <exec command="rm -Rf /var/www/html/${phing.project.name}/*" />
        <exec command="cp -R '/var/lib/jenkins/jobs/pomodoro/workspace' /var/www/html/${phing.project.name}" />
    </target>
</project>